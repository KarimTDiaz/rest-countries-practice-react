const API_URL = 'https://restcountries.com/v3.1/';

export const API_REQUEST = {
	all: `${API_URL}all`,
	africa: ` ${API_URL}region/africa`,
	america: ` ${API_URL}region/america`,
	asia: ` ${API_URL}region/asia`,
	europe: `${API_URL}region/europe`,
	oceania: `${API_URL}region/oceania`,
	name: `${API_URL}name/`
};
